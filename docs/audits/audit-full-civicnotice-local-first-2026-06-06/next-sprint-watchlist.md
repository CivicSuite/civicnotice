# Next sprint watchlist

- Add live CivicLegal handoff tests when that downstream module exists.
- Add publication proof/write-back boundary tests when real external publication integrations are introduced.
- Preserve both denied and keyed staff queue evidence in the clean-machine suite gate.
